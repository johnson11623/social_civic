/**
 * Switches for features that are built but not shown yet. Flip one to bring
 * the feature back; the code and its tests stay in place.
 */
export const FEATURES = {
	/** "Why am I seeing this?" on posts and elevation banners (T-W1.4.1.5). Hidden for now. */
	whyAmISeeing: false,
} as const;
